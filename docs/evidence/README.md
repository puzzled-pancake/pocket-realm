# Evidence

This directory holds the committed evidence artifacts required by
`docs/plans/rp-depth-fix-plan-v2.3.md` §14: the sanitized RP session
transcript with its symptom→line→code-anchor index
(`rp-session-transcript-2026-09-05.md`, whose gitignored source lives at
`tmp/rp_session_transcript_2026-09-05.log`), the T5 before/after
transcript scorecards (versioned filenames, to be committed by the T5
run), and the §15 round-robin review log (`review-rounds.md`, appended
by the review runner until a round records 8/8 PASS).
