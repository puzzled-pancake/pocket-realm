# Gladio diagnostic D2 — suppress GL_ARB_fragment_program advertisement
# Status: DIAGNOSTIC A/B ONLY; DO NOT SHIP AS THE PRODUCTION DEFAULT
# Issue ID: A10
# Behavioral change: remove only GL_ARB_fragment_program from the advertised extension string for one rendering comparison.
# Interpretation: if black materials become textured (even with degraded fog/water), the ARB fragment-program conversion/state path is implicated.
# Protocol impact: NONE.
# Build/hash impact: SERVER diagnostic build ID and SHA-256 must change.
# Rollback: reverse this patch immediately after the A/B run.

--- a/09_server_gl_context.c_DEPLOYED
+++ b/09_server_gl_context.c_DEPLOYED
@@ -971,7 +971,8 @@
         "GL_ARB_internalformat_query2 GL_ARB_map_buffer_range "
         "GL_ARB_multitexture GL_ARB_texture_env_combine GL_EXT_texture_env_combine "
         "GL_ARB_texture_env_dot3 GL_EXT_texture_env_dot3 GL_ARB_shader_objects "
-        "GL_ARB_vertex_program GL_ARB_fragment_program GL_ARB_color_buffer_float "
+        /* DIAGNOSTIC BUILD ONLY: suppress ARB fragment programs for one A/B run. */
+        "GL_ARB_vertex_program GL_ARB_color_buffer_float "
         "GL_ARB_occlusion_query";
 
     if (numExtensions == 0) {
