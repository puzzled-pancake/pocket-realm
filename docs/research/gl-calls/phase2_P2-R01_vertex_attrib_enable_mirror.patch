# Gladio Phase 2 — P2-R01 generic vertex-attrib enable mirror
# Status: PRODUCTION CANDIDATE
# Issue IDs: A11 (also supports A10 diagnostics)
# Behavioral change: glEnableVertexAttribArray now updates the client VAO mirror symmetrically with glDisableVertexAttribArray before forwarding.
# Protocol impact: NONE.
# Build/hash impact: CLIENT libGL.so.1 build ID and SHA-256 must change.
# Rollback: reverse this one-line state-mirror change.

--- a/05_client_gl_calls.c_DEPLOYED_V5
+++ b/05_client_gl_calls.c_DEPLOYED_V5
@@ -1723,6 +1723,7 @@
 
 void glEnableVertexAttribArray(GLuint index) {
     GL_CALL_LOCK();
+    GLVertexArrayObject_setAttribState(currentGLContext->clientState, index, VERTEX_ATTRIB_ENABLED, false);
     gl_send(currentGLContext->serverRing, REQUEST_CODE_GL_ENABLE_VERTEX_ATTRIB_ARRAY, &index, sizeof(GLuint));
     GL_CALL_UNLOCK();
 }
