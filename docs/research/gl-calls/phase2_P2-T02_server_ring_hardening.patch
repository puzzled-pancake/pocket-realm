# Gladio Phase 2 — P2-T02 server ring hardening
# Status: PRODUCTION CANDIDATE
# Issue IDs: B03, B06
# Behavioral change: activate existing server peer-fd liveness checks; make macro commits survive uint32 head/tail wrap to exactly zero; terminate instead of continuing after wrap-buffer allocation failure.
# Protocol impact: NONE.
# Build/hash impact: SERVER libgladiorenderer.so build ID and SHA-256 must change.
# Rollback: reverse this patch and restore the previous server binary/hash.

--- a/09_server_gl_context.c_DEPLOYED
+++ b/09_server_gl_context.c_DEPLOYED
@@ -523,9 +523,11 @@
 
     context->serverRing = RingBuffer_create(shmFds[0], SERVER_RING_BUFFER_SIZE);
     if (!context->serverRing) goto error;
+    RingBuffer_setPeerFd(context->serverRing, clientFd);
 
     context->clientRing = RingBuffer_create(shmFds[1], CLIENT_RING_BUFFER_SIZE);
     if (!context->clientRing) goto error;
+    RingBuffer_setPeerFd(context->clientRing, clientFd);
 
     int result = send_fds(clientFd, shmFds, 2, NULL, 0);
     if (result < 0) goto error;
--- a/19_server_ring_buffer.h_SHARED
+++ b/19_server_ring_buffer.h_SHARED
@@ -2,6 +2,7 @@
 #define WINLATOR_RING_BUFFER_H
 
 #include <stdatomic.h>
+#include <stdbool.h>
 #include <stdint.h>
 
 #include "events.h"
@@ -49,6 +50,7 @@
 #define RING_READ_BEGIN(ring, data, size) \
     uint32_t ringHead = 0; \
     void* ringData = NULL; \
+    bool ringReadReady = false; \
     do { \
         if (size == 0 || !RingBuffer_waitForRead(ring, size)) break; \
         ringHead = RingBuffer_getHead(ring); \
@@ -58,12 +60,17 @@
         } \
         else { \
             data = malloc(size); \
+            if (!data) { \
+                RingBuffer_setStatus(ring, RING_STATUS_EXIT); \
+                break; \
+            } \
             uint32_t start = ring->bufferSize - ringOffset; \
             memcpy(data, ring->buffer + ringOffset, start); \
             memcpy(data + start, ring->buffer, size - start);\
             ringData = data; \
         } \
         ringHead += size; \
+        ringReadReady = true; \
     } \
     while (0)
 
@@ -73,7 +80,7 @@
             free(ringData); \
             ringData = NULL; \
         } \
-        if (ringHead > 0) RingBuffer_setHead(ring, ringHead); \
+        if (ringReadReady) RingBuffer_setHead(ring, ringHead); \
     } \
     while (0)
 
@@ -82,6 +89,7 @@
     void* ringData = NULL; \
     uint32_t ringDataSize = 0; \
     uint32_t ringOffset = 0; \
+    bool ringWriteReady = false; \
     do { \
         if (size == 0 || !RingBuffer_waitForWrite(ring, size)) break; \
         ringTail = RingBuffer_getTail(ring); \
@@ -91,9 +99,14 @@
         } \
         else { \
             ringData = malloc(size); \
+            if (!ringData) { \
+                RingBuffer_setStatus(ring, RING_STATUS_EXIT); \
+                break; \
+            } \
             ringDataSize = size; \
         } \
         ringTail += size; \
+        ringWriteReady = true; \
     } \
     while (0)
 
@@ -106,7 +119,7 @@
             free(ringData); \
             ringData = NULL; \
         } \
-        if (ringTail > 0) RingBuffer_setTail(ring, ringTail); \
+        if (ringWriteReady) RingBuffer_setTail(ring, ringTail); \
     } \
     while (0)
 
