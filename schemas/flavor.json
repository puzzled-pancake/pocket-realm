{
  "schema": 1,
  "reference_document": "docs/wiki/README.md",
  "flavor": "offline-vanilla-1.12",
  "description": "The first releasable product flavor: a self-contained, offline-only PvE Vanilla 1.12 realm on Android. This manifest is the contract the build, manifest merger, and release-acceptance checks enforce.",
  "target_device_profile": {
    "primary": "Retroid Pocket 6 12 GB",
    "native_abi": "arm64-v8a",
    "min_sdk": 26,
    "target_sdk": 27,
    "page_size": "16 KB compatible (no exclusive core-type assumptions)",
    "development_abi": "x86_64 on the fixed emulator lanes recorded under tests/avd/"
  },
  "game": {
    "expansion": "Vanilla / Classic",
    "client_version": "1.12.1 build 5875 (user-supplied; PE/build fast scan on first connect and full managed import)",
    "supported_client_builds": [],
    "notes": "supported_client_builds is populated once exact client hashes are qualified. Empty means not yet qualified; import must reject until at least one entry exists."
  },
  "includes": {
    "android_app": "Kotlin/Compose launcher, supervisor, wizard, foreground service, controller bridge",
    "native_realm": "Supervised native MariaDB, realmd, mangosd, and Playerbots components; x86_64 development then arm64-v8a parity",
    "persistence": "App-private MariaDB datadir/socket, migration ledger, dirty recovery, and database-consistent verified backups",
    "client_import": "Build-5875 validation plus resumable managed copy and checkpointed DBC/maps/vmaps/mmaps preparation",
    "runtime_provider": "x86DirectWine first; ARM Box64/Wine WoW64 after integrated x86; alternative runtimes are outside the release critical path",
    "addons": "Minimal project-owned controller/status/bot UI plus curated reversible profiles"
  },
  "excludes_from_release_artifact": {
    "proprietary_client_files": "WoW.exe, MPQs, DBCs, maps, models, textures — never bundled; user imports them",
    "connected_code": "No connected transport, Realm Kernel, replication, or membership code in the offline APK",
    "connected_permissions": "No INTERNET-dependent authority, P2P, or relay permissions",
    "hidden_online_switch": "No runtime flag re-enables connected behavior"
  },
  "android_permissions": {
    "required": [
      "INTERNET",
      "FOREGROUND_SERVICE",
      "FOREGROUND_SERVICE_SPECIAL_USE",
      "POST_NOTIFICATIONS",
      "WAKE_LOCK"
    ],
    "foreground_service_type": "specialUse (long-running local realm server; dataSync is capped at 6h on Android 15 and unsuitable for multi-hour play)",
    "storage": "Scoped storage / SAF for client import and exports only",
    "prohibited_in_offline_flavor": [
      "External gameplay authority",
      "P2P or relay transport",
      "ACCESS_NETWORK_STATE used for realm decisions"
    ],
    "network_policy": "INTERNET permits app-local TCP sockets and optional explicitly configured update checks; offline play binds realm endpoints to 127.0.0.1 and has no external authority dependency."
  },
  "release_acceptance_link": "docs/wiki/Build-and-Packaging.md",
  "rule": "The offline flavor is a hard boundary. Connected-realm work starts only after the offline flavor is released and lives in a separate flavor/build type."
}
